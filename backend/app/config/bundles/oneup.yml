oneup_uploader:
    mappings:
        gallery:
            root_folder: false
            frontend: dropzone
            namer: app.upload_helper
            storage:
                directory: "%kernel.root_dir%/../web/uploads/"

        files:
            frontend: dropzone
            namer: app.upload_helper
            storage:
                directory: "%kernel.root_dir%/../storage/files/"
