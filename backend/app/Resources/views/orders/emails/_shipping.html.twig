{% if 'self' != order.shippingRules.type %}

    <table class="width-total info">
        <tr class="line-info info-extra">
            <th class="position item-size">
                Frete
            </th>
            <th class="position">
                {{ 'sices' == order.shippingRules.type ? order.shipping|currency : "INCLUSO" }}
            </th>
        </tr>
    </table>

{% endif %}
