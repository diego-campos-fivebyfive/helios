<table class="width-total info">
    {% if order.total != order.totalExcDiscount and order.discount %}
        <tr class="line-info info-extra">
            <th width="40%" align="left">
                DESCONTO COMERCIAL
            </th>
            <th align="left">
                -{{ order.discount|currency }}
            </th>
        </tr>
    {% endif %}
    {% if order.coupon %}
        <tr class="line-info info-extra">
            <th width="40%" align="left">
                CUPOM DE DESCONTO
            </th>
            <th align="left">
                -{{ order.coupon.amount|currency }}
            </th>
        </tr>
    {% endif %}
</table>
