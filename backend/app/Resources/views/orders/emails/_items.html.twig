{% set showPrices = not item.ispromotional and not item.isFiname %}
<table class="width-total orders" cellspacing="0">
    <thead>
    <tr class="th">
        <th>Código</th>
        <th style="min-width: 45%;">Produto</th>
        {% if showPrices %}<th>Custo Un.</th>{% endif %}
        <th>QTD</th>
        {% if showPrices %}<th>Custo T.</th>{% endif %}
    </tr>
    </thead>
    <tbody>

    {% for element in item.elements %}
        <tr class="td">
            <td>{{ element.code }}</td>
            <td class="text-left">{{ element.description }}</td>
            {% if showPrices %}<td>{{ element.unitPrice|currency }}</td>{% endif %}
            <td class="text-center">{{ element.quantity }}</td>
            {% if showPrices %}<td>{{ element.total|currency }}</td>{% endif %}
        </tr>
    {% endfor %}

    </tbody>
</table>
