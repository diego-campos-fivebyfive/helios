{% macro render_result(products) %}

    {#{% set products = pagination.items %}#}

    <table id="table_catalog" data-filter="#catalog_filter" class="table table-striped" data-page-size="5">
        <thead>
        <tr>
            <th> {{ 'Name'|trans }} </th>
            <th> {{ 'Description'|trans }} </th>
            <th>&nbsp;</th>
        </tr>
        </thead>
        <tbody id="render_result">

        {% for product in products %}

            <tr>
                <td> {{ product.name }} </td>
                <td> {{ product.description }} </td>
                <td>
                    <a onclick="add_to_catalog('{{ path('catalog_add',{'slug':product.slug}) }}', this);" class="btn btn-sm btn-success">
                        <i class="fa fa-check"></i> {{ 'Add To Catalog'|trans }}
                    </a>
                </td>
            </tr>

        {% endfor %}

        </tbody>
        <tfoot>
        <tr>
            <td colspan="3">
                <div class="pagination-centered">
                    <ul class="pagination"></ul>
                </div>
                {#<div class="btn-group">
                    {{ knp_pagination_render(pagination) }}
                </div>#}
            </td>
        </tr>
        </tfoot>
    </table>

{% endmacro %}

{% import _self as this %}

{% if not ajax %}

    <script>

        var requesting = false,
            render_result = $('#render_result');

        function request_catalog(config){

            if('object' != typeof config.data){
                config.data = {};
            }

            if(!requesting) {
                requesting = true;

                var data = $.extend({ajax:true}, config.data, true);

                $.ajax({
                    url: config.url,
                    method: 'get',
                    data: data,
                    success: function (response) {
                        render_result.html(response);
                        requesting = false;
                    }
                });
            }
        }

        function init_paginator(){

            var page_links = render_result.find('ul.pagination').find('li>a');

            page_links.on('click', function (e) {

                e.preventDefault();
                e.stopPropagation();

                request_catalog({
                    url: $(this).attr('href')
                });

            });
        }

        $('#form_search').on('submit', function(event){
            event.preventDefault();

            requesting = false;
            request_catalog({
                url: $(this).attr('action'),
                data: {search: $(this).find('[name="search"]').val()}
            });

        });

        function add_to_catalog(url, e){
            var link = $(e), row = link.closest('tr');

            $.ajax({
                url: url,
                method: 'post',
                success: function(response){
                    console.log(response);
                }
            });
        }

    </script>


    <div class="wrapper wrapper-content animated fadeInRight">

        <div class="row">
            <div class="col-lg-12">
                {#<div class="ibox float-e-margins">#}

                    {#<div class="ibox-title">
                        <h5> {{ 'Avalilable Products'|trans }} </h5>
                        <div class="ibox-tools">
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                                <i class="fa fa-wrench"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-user">
                                <li><a href="#">Config option 1</a>
                                </li>
                                <li><a href="#">Config option 2</a>
                                </li>
                            </ul>
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>#}

                    <div class="ibox-content">
                        <div class="row">

                            {#
                            <div class="col-sm-5 m-b-xs"><select class="input-sm form-control input-s-sm inline">
                                    <option value="0">Option 1</option>
                                    <option value="1">Option 2</option>
                                    <option value="2">Option 3</option>
                                    <option value="3">Option 4</option>
                                </select>
                            </div>
                            <div class="col-sm-4 m-b-xs">
                                <div data-toggle="buttons" class="btn-group">
                                    <label class="btn btn-sm btn-white"> <input type="radio" id="option1" name="options"> Day </label>
                                    <label class="btn btn-sm btn-white active"> <input type="radio" id="option2" name="options"> Week </label>
                                    <label class="btn btn-sm btn-white"> <input type="radio" id="option3" name="options"> Month </label>
                                </div>
                            </div>
                            #}

                            <div class="col-sm-4">
                               {# <form id="form_search" action="{{ path('catalog_global',{'page':''}) }}">#}
                                <div class="input-group">
                                    <input name="search" type="text" id="catalog_filter" placeholder="Search" class="input-sm form-control">
                                    <span class="input-group-btn">
                                        <button type="submit" class="btn btn-sm btn-primary">
                                            {{ 'Search'|trans }}
                                        </button>
                                    </span>
                                </div>
                                {#</form>#}
                            </div>

                        </div>
                        <div id="render_result" class="table-responsive">
                            {{ this.render_result(products) }}
                        </div>
                    </div>

                {#</div>#}

            </div>
        </div>
    </div>

{% else %}

    {{ this.render_result(pagination) }}

{% endif %}

<script> $('#table_catalog').footable(); </script>