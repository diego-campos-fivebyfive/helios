{% extends view('layout') %}

{% block body %}

    <div class="wrapper wrapper-content animated fadeInRight">

        {% include view('project.breadcrumbs') %}

        <div class="row">
            <div class="col-lg-12">
                <div class="ibox">
                    <div class="ibox-content">
                        <div class="row">
                            <div class="col-md-12">

                                {% if 'project_metadata' == tag %}

                                    <div class="alert alert-danger">
                                        <i class="fa fa-exclamation-triangle"></i>
                                        O projeto selecionado não foi calculado na fase anterior
                                        <a class="alert-link" href="{{ path('project_update',{token:project.token}) }}">
                                            Clique aqui para voltar ao dimensionamento
                                        </a>.
                                    </div>

                                {% endif %}

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>

{% endblock %}